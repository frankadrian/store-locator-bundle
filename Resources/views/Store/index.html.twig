{% extends '@FrankStoreLocator/base.html.twig' %}

{% block body -%}
    <h1>Store locator</h1>

    <div class="wrapper" style="padding: 0; margin:0;width: 100%; height: 100%;  ">
        {{ storelocator.input() }}
        {{ storelocator.map() }}
    </div>

    <div>
        <a href="{{ url('admin_store_new') }}">Create Store</a>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('bundles/frankstorelocator/js/storelocator.js') }}"></script>
{% endblock javascripts %}
